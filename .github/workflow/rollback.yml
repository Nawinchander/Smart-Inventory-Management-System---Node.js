name: Rollback

on:

  workflow_dispatch:

jobs:

  rollback:

    runs-on: ubuntu-latest

    steps:

      - uses: azure/setup-kubectl@v4

      - run: |
          kubectl rollout undo \
          deployment inventory-deployment